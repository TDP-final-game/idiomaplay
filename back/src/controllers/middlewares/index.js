'use strict';

// This file doesn't make much sense now, but it will be useful in case we want
// to add more middlewares.

const authentication = require('./authentication');

module.exports = authentication;
